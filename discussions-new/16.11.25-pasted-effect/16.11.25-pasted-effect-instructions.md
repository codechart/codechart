I want to create an effect of circles going over the pasted LLM text

## existign code
1. look at .../ui/.../llmJsonActions.ts (that loads pasted json into chart), 
2. and at .../ui/.../chart.styling.ts -> handleBeforeDrawEvent method for how to draw a circle on the canvas

## what we want
you can put a timeout that waits for everything to load, then runs a expanding circle effect one by one on loaded nodes
