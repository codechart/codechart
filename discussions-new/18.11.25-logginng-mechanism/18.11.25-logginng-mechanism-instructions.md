## Current situation
A. we can now download logs from the UI, and api logs and vscode extension logs are logged to console.
    1 the api runs from an exe file in the user machine
    2 the UI runs from same file, uses localhost
    3 the vscode extension utizlizes a webview displaying the UI, and using the API to run searches on code
    4 . the UI can run in ide (look at packages/ui/../ideConnect.ts), or in the browser. a flag is used to differentiate where its running
B. when loading the extension a file is created called cochart.group.md in the project folder of the use

## what i want to achieve
### A. A unified logging system
1. when running in ide: all systems(api/extension/ui) should write logs to a file in the user project, under folder /cochart (created by the extension)
2. when running in borwser: all systems(api/ui) should write logs to /logs folder where the api is running
3. the logs files shouldave a limited size, cycling through the logs

## context
look at @packages/api/src/App.ts ,  @packages/vscode-plugin/dist/panelWebviewProvider.js, @packages/ui/src/app/app.component.ts, packages/ui/../ideConnect.ts

