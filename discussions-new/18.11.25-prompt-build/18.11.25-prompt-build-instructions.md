## Current situation
A. user can copy promts from the ui. 
B. there i also a read and write prompt that is copied to build folder, 
C. there are also claude commands, 
## what i want to achieve
on build, the command from claude is copied to the build resources, but also when user copies the prompt from the UI it should get the same promt

in other words, the claude command is the latest most accurate version, and the user should see/use them

## context
ui promts: the promts are located in ../ui/../llmActions.ts
build promts: look for ../api/build-resouces
claude command (correct version): under /.claude/commands