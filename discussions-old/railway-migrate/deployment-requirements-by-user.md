we have 4 services
UI is built and served as API as static html
API is built into runnables
these runnables are downloadable from the landing page.
the api/ui runs only locally on the users computer.
we also have 2 IDE plugins

we have a license api that communicates with the local api on the user machine for audit and license. it uses posgresql
we dont need the docs

we're looking to migrate to railway, with minimal code changes.
download button is crucial! - the product works locally

