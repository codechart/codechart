i want to check if we can replace diffLines method with,
see @discussions/synch-issue/patience-diff.ts and @discussions/synch-issue/PatienceDiff-dev/, 
you can also use context7 that has documentation for this
the git url is https://github.com/jonTrent/PatienceDiff, https://github.com/jonTrent/PatienceDiff.git

the task is to
1. understand how this works
2. then create a separate file for diffline and test it (its nly for getting moved/changed/deleted lines). use the same test data we have now
3. when we see that its working, we integrate this file into our project and call it
4. we then continue tests as before (test.js) until the tests pass


what questions do you have?