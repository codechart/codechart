{
  "name": "cochart-api-tests",
  "version": "1.0.0",
  "type": "module",
  "description": "API tests for Cochart search endpoints",
  "scripts": {
    "test": "NODE_OPTIONS='--import tsx' mocha 'search.test.ts' --timeout 10000",
    "test:watch": "mocha --loader ts-node/esm 'search.test.ts' --watch --timeout 10000"
  },
  "devDependencies": {
    "@types/chai": "^4.3.0",
    "@types/mocha": "^10.0.0",
    "@types/node": "^20.0.0",
    "chai": "^4.3.0",
    "mocha": "^10.2.0",
    "ts-node": "^10.9.0",
    "tsx": "^4.21.0",
    "typescript": "^5.0.0"
  }
}
